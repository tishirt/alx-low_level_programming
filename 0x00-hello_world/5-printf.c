#include <stdio.h>
/**
 * main -A program that prints a line using printf followd with a new line
 * Return: 0 (succes)
 */
int main(void)
{
	printf("with proper grammar, but the outcome is a piece of art,\n")
	return (0);
}
